<?php

namespace generate;

interface generate {
    public static function generate(string $name): void;
}

class generateEntity Implements generate {
    public static function generate(string $command_args): void {
        
    }
}